import PropertyEditorDropdown from './src/main.vue';

/*引用 */
PropertyEditorDropdown.install = function(Vue) {
  Vue.component(PropertyEditorDropdown.name, PropertyEditorDropdown);
};

export default PropertyEditorDropdown;
