import PropertyEditorCells from './src/main.vue';

/*引用 */
PropertyEditorCells.install = function(Vue) {
  Vue.component(PropertyEditorCells.name, PropertyEditorCells);
};

export default PropertyEditorCells;
