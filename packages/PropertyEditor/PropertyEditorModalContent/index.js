import PropertyEditorModalContent from './src/main.vue';

/*引用 */
PropertyEditorModalContent.install = function(Vue) {
  Vue.component(PropertyEditorModalContent.name, PropertyEditorModalContent);
};

export default PropertyEditorModalContent;
