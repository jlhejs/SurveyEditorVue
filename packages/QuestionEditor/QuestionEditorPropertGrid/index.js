import QuestionEditorPropertGrid from './src/main.vue';

/*引用 */
QuestionEditorPropertGrid.install = function(Vue) {
  Vue.component(QuestionEditorPropertGrid.name, QuestionEditorPropertGrid);
};

export default QuestionEditorPropertGrid;
